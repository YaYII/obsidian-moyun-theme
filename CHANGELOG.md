# 变更日志

本项目的版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.3] - 2026-09-18

### 图表方框改为「有色轮廓 + 透明内里」

用户反馈：放大看图时，方框里都有底色。要求是**线框有颜色用于区分，框里不要背景色**。

- 六种风格里的节点形状（`.node rect/circle/polygon`、时序图的 `.actor`、状态图与类图的
  `stateGroup` / `classGroup` / `entityBox`、便签 `.note`）一律 `fill: transparent`，
  只保留强调色描边与文字色。
- **边标签是唯一例外**：它必须保留不透明底色，否则从标签下方穿过的连线会把文字切开。
  底色从「纸面色」改为 `--background-primary`，于是无论在笔记里还是在查看器里都与所在背景
  一致，不再出现「比周围亮一块」。
- 饼图扇区、甘特条、坐标轴这些**用颜色编码数据**的图形保持填充不变 —— 透明化它们等于丢信息。

回归防线：`tools/check-mermaid.mjs` 增加契约断言 —— 六种风格 × 明暗两种模式下，节点形状的
`fill` 透明度必须为 0，边标签底色的透明度必须为 1。**12 组全部通过**，文字对比度依旧全部
≥ 4.5:1（透明后按所在画布重新计算）。

## [1.2.2] - 2026-09-18

### 修复：Canvas（白板）卡片里的图改回「缩放进屏」

1.2.1 让手机版心里的宽图保持原始尺寸、横向滑动，但这条策略不能无脑套到白板卡片上：
卡片往往只有 240~320px 宽、内部不能平移，图若保持原始尺寸会被裁掉；而整块画布本身可以
双指缩放（Canvas 由 Cytoscape 渲染，bundle 里就有 `plugins.canvas.label-pinch-zoom` 这个
手势提示）。于是定下一条界线：

> **有缩放能力的地方就缩放进屏（放大交给手势）；没有缩放能力的地方才需要「原尺寸 + 横向滑动」。**

实现：`body.is-mobile .canvas-node-content .mermaid svg { max-width: 100% }`。验证台新增
Canvas 卡片夹具（窄卡片 + 900px 图，刻意无文字以免扰动对比度断言），check-mobile 增加断言
「卡片内图渲染宽度 ≤ 卡片宽度」（实测 241 / 271 / 311 / 620px，全部等于卡片宽）。
**111 条断言全绿。**

### 文档：白板是核心功能，不需要插件

README 中英两节补充说明：Canvas 是 Obsidian **核心**功能（设置 → 核心插件 → 白板，
i18n 命名空间 `plugins.canvas.*`），把笔记拖进白板即成为文件卡片、整块画布可双指缩放 ——
这是手机上放大看图的推荐姿势。而「把阅读视图本身变成可缩放的白板」必须由插件实现：
主题是纯 CSS，无法创建视图、控件或手势。

## [1.2.1] - 2026-09-17

### 修复：手机上的宽图不再被压扁（「图看不清楚」的真正成因）

用户追问「为什么手机上不能双指放大缩小，这样很多图会看不清楚」。先把缩放这件事的机制查实：

- Obsidian 是 Capacitor 应用，其 `zoomEnabled` 选项**默认 false**（官方描述：Enable zooming
  within the Capacitor Web View）—— 属 **app 级原生开关**，样式表永远碰不到；
- 它的 HTML 里还写死了 `maximum-scale=1.0, user-scalable=no`（`obsidian.asar` 可直接 grep 到）；
  MDN 指出这类声明「浏览器设置可以忽略，iOS 10+ 默认忽略」，所以 iOS 本来就允许捏合，
  Android 则由 WebView 原生设置决定；
- 反证：`obsidian.asar` 里唯一实现触摸缩放的是 Cytoscape（`t.pinching`、`gestureStartHandler`、
  `viewport({zoom, pan, cancelOnFailedZoom: true})`），因此**关系图与白板里双指缩放可用**，
  而正文是普通 WebView 页面，没有自建捏合处理。

**主题无法开启双指缩放**（已写进 README 中英两节与 docs/zh），但可以让「图看不清」不再发生 ——
而这恰恰是主题自己的锅：`src/60-plugins/mermaid.css` 给 `.mermaid svg` 设了
`max-width: 100%`，桌面端是对的，手机上却把 900px 的流程图压成 340px，图内 14px 文字实际
只剩 5.3px。现在手机上改为**保持原始尺寸 + 区块横向滑动**（含
`-webkit-overflow-scrolling: touch`），并提供 Style Settings 开关
「✨ 内容增强 → 手机上图表缩放进屏」回到旧行为。

### 验证

`tools/check-mobile.mjs` 增加宽图档位：验证台新增一张 900×200 的几何夹具（刻意不含文字，
避免扰动 verify.mjs 的对比度断言），断言手机上「图渲染宽度 ≥ 880px」「容器
`scrollWidth > clientWidth`，即可横向滑动」「打开设置项后图收进版心」。**107 条断言**全部通过；
其余关卡（build --check / npm test / verify / check-mermaid / conflicts:strict 0）同样全绿。

## [1.2.0] - 2026-09-17

### 修复：移动端（手机/平板）的阅读密度与字号归属

用户反馈「手机上看笔记，字很大、一屏放不下多少内容，还不能双指缩放」。这句话里混着
三件不同的事，因此先用**差分实测**把它们分开，再动代码：新增 `tools/check-mobile.mjs`，
在同一个手机视口下分别加载「不加载主题」（Obsidian 原生基线）与「加载主题」，逐项对比
字号 / 版心 / 每行字数 / 一屏行数 / 横向溢出。实测（390×844，正文 16px）：

| 项目 | Obsidian 原生 | 墨韵（改前） | 墨韵（改后） |
|---|---|---|---|
| 正文字号 | 16px | 16px | 16px |
| 每行汉字 | 21.4 | 21.4 | 21.4 |
| 版心上下留白 | 8px | **40px** | **16px** |
| 一级标题 | 25.9px（公文模式 32px） | 32px | **28px** |
| 一屏可见行数 | 35.2 | 29.3 | 29.3 |

三条用证据换来的结论：

1. **字号不是主题放大的**：手机端正文与原生完全一致（16px）。手机上的字号来自设备自己的
   「设置 → 外观 → 字体大小」，它写在 `appearance.json` 里、桌面与手机会一起同步；想让两端
   不一致，就得让同步工具忽略这个文件。
2. **双指缩放是 Obsidian 自己关掉的**：Obsidian 打包的 HTML 里写死了
   `<meta name="viewport" content="… maximum-scale=1.0, user-scalable=no …">`。CSS 改不了
   viewport 声明，所以**任何主题都不可能打开它**。主题能做且已做的只有两件事：确认自己的规则
   没有吃掉手势（实测 `touch-action` 未被主题声明），以及让字号跟着 Obsidian 自己的设置走。
3. **主题确实占用了手机屏幕**：桌面级的 40px 上下留白、32px 公文标题，在 390px 宽的屏上等于
   白扔两行版面、标题一行放不下 12 个汉字。现在这两处只在移动端收敛（`body.is-mobile`）：
   留白 40px → 16px，h1 2em → 1.75em、内联标题 2.1em → 1.8em、h2/h3 → 1.45em/1.25em。

顺带修正一处文档错误：`docs/zh/README.md` 把 `minAppVersion` 写成了 1.6.0（实际自 1.1.7 起
就是 1.13.0）。

### 新增：手机字号微调（Style Settings）

`my-mobile-font-delta`（-4 ~ +4 px，默认 0）：只作用于手机/平板，在 Obsidian 自带字号上叠加
偏移，桌面端不受影响。对应「桌面与手机共用同一份 `appearance.json`，但手机需要小一号字」这个
具体场景。

### 验证

`node tools/check-mobile.mjs` 现有 **95 条断言**（5 种视口 × 3 种变体），覆盖：正文不被放大、
无横向溢出、一屏行数下限、标题相对正文的倍数上限、版心留白收敛、`text-size-adjust` 生效值 =
100%、`touch-action` 未被吃掉、「手机字号微调」旋钮真的接通（手写变量验证 +2px 生效），
并与原生基线逐项对比；桌面视口作为对照组（16px → 16.5px 的宽屏自适应增益仍在）。

一处已知且不修的现象：桌面视口下侧栏折叠图标有 4px 向左贴边溢出（`left:-4`），被父容器裁掉、
肉眼不可见，验证台把它记为信息而不是失败。

## [1.1.9] - 2026-09-16

### 新增：图表风格展示图（README 中英两节）

用户要求把六种图表风格的效果图做出来并放进仓库。新增
`tools/make-mermaid-gallery.mjs`，用**真实 Mermaid 包 + Obsidian 的真实初始化参数**
渲染同一张流程图（含子图、换行标签、带标签的连线），每种风格一张，共七张：

| 文件 | 风格 |
|---|---|
| `mermaid-ink-dark.webp` / `mermaid-ink-light.webp` | 墨韵（跟随主题色，明暗各一张） |
| `mermaid-cyber.webp` | 赛博 · 青蓝 |
| `mermaid-cyber-matrix.webp` | 赛博 · 矩阵翠绿 |
| `mermaid-cyber-violet.webp` | 赛博 · 紫粉 |
| `mermaid-cyber-amber.webp` | 赛博 · 琥珀 |
| `mermaid-line-light.webp` | 极简 · 黑白线框 |

两个决定：

1. **输出到 `docs/images/mermaid/` 而不是 `tools/preview/out/`** —— 后者是验证产物、
   被 `.gitignore` 排除，而 README 需要的是可提交、可长期引用的展示图。
2. **借 Chromium 的 canvas 转成 WebP** —— 七张 2x PNG 加起要 1.5 MB 以上，
   转 WebP 后合计 396 KB（单张 38–73 KB）。仓库里不该放这么大的 PNG。

展示图刻意用真实笔记里常见的图形（子图 + `<br/>` 换行 + 带标签的连线），
而不是验证台夹具那种极简图 —— 展示图要让人判断「我要不要用这个风格」。

README 英文小节给出完整七张（两列配对 + 说明文字），中文小节给三张代表
（墨韵夜间 / 赛博青蓝 / 极简线框）并指向英文小节的完整图廊。
## [1.1.8] - 2026-09-16

### 优化：产物压缩空白，261.7 KB → 236.9 KB（累计从 487 KB 降 51%）

目录仍然提示 `Theme CSS file is larger than recommended` —— 说明阈值**低于 262 KB**。
阈值官方没有公开（校验器不开源，论坛也无人给出数字），只能往「尽量小」做。
这一版做的是**不改变语义的空白压缩**，也就是分发样式表的常规做法：

- 去掉行首缩进与空行；
- 声明行去掉冒号后的空格、去掉块尾多余分号；
- 折叠选择器与花括号之间的空白。

**刻意不做**的三件事：不改写数值（`0px`→`0` 会在 `calc()` 里失效）、不重命名自定义属性
（那会毁掉可读性与调试能力）、不合并规则（有改变层叠顺序的风险）。

判定「这行是声明」的依据是**以分号结尾**：选择器行永不以分号结尾，因此伪类选择器
（`a:hover,`）不会被误当成声明改坏。

### 修复：压缩过程中自作自受的一个严重 bug（已加检查）

第一版压缩函数对**每一行**都做 `trim()` —— 而 `@settings` 的 YAML 正是靠缩进表达结构。
产物里那个块被压成了一行 `/* @settingsname: MoYun（墨韵）id: moyunsettings:…`，
**Style Settings 会彻底失效**（整个设置面板清空）——比体积告警严重得多。

修法：注释块在压缩时**原样保留**（含缩进与换行）。
并新增构建期检查把这条不变式钉死：

> 产物里的 `@settings` 块必须与源码**逐字符一致**，否则构建失败。

### 修复：保留注释的规则过宽（白多留十几 KB）

上一版的剥离器写成「只要注释里出现『模块：』就保留」，结果源码里凡是提到模块名的
说明性注释全被带进了产物。改为**精确匹配构建生成的横幅**（`/\* #### 模块：…`）。

### 新增：`--pretty` 开关

`node build.mjs` —— 默认产出压缩版（分发形态）；
`node build.mjs --pretty` —— 保留缩进换行；
`node build.mjs --comments` —— 保留全部注释并保留格式（对照源码阅读）。

验证：`npm test 28` / `verify 59×2`（真实 DOM 计算样式，压缩若改坏任何选择器这里必然失败）/
`check-mermaid` / `conflicts` / `name` / `workflow` / 产物陈旧检查 全过；
另用 postcss 与真实 js-yaml 复核产物：解析通过、36 个设置项完好。
## [1.1.7] - 2026-09-16

### 修正：`minAppVersion` 从 1.6.0 改为 1.13.0（原来那个是假的）

目录校验连续提示 `css-text-indent` / `css-text-justify` / `text-decoration` 在
**Obsidian 1.5.8** 上「仅部分支持 / 不支持」。顺着这条线检查才发现一个更根本的问题：

**主题声明的 `minAppVersion: 1.6.0` 是错的。** 墨韵的核心能力建立在 CSS Text Level 4 上 ——
`text-autospace`（中西文自动间距）需要 **Chromium 136+**，而 1.6.0 时代的 Electron 根本
不认识这条属性。也就是说：在 1.6.0 上装墨韵，不会报错、不会坏版面，但**中文化的一半意义
会静默消失** —— 这正是最难排查的那种「装上了却没效果」。

现在声明为 **1.13.0**，与两件事一致：

1. 主题**真正需要**的运行时（Chromium 136+ 起才有那批特性）；
2. 本项目**实际验证**的版本（本机 Obsidian 1.13.7 / Chromium 150，参考 CSS 亦从此版本提取）。

README 新增「兼容性」小节（中英各一份），把「为什么是 1.13」讲清楚，
并说明：主题只使用**公开 CSS 变量**与标准属性；若将来 Obsidian 改名某个内部变量，
受影响的规则会回落到 Obsidian 自身默认值，而不是把版面弄坏。

### 关于那三条浏览器特性告警

它们以 **Obsidian 1.5.8** 为基线，属于校验器底表（caniuse 类）的产物：

| 告警 | 实情 |
|---|---|
| `css-text-indent` 仅部分支持 | 该底表对 `text-indent` 的特殊取值（如 `hanging`）标注为部分支持；我们只用常规数值/`em` |
| `css-text-justify` 不支持 | 我们只在公文模式下用 `text-justify: inter-character`；Chromium 支持该取值，底表未覆盖 |
| `text-decoration` 仅部分支持 | 我们只用最基础的 `none / underline` |

这三条是**提示级**，不影响通过；把最低版本声明改诚实之后，它们的意义也更清楚：
在 1.13+ 的目标运行时上，这些属性都是完全可用的。
## [1.1.6] - 2026-09-16

### 优化：产物剥离说明性注释，体积 487 KB → 262 KB（-46%）

目录校验提示 `Theme CSS file is larger than recommended`。量了一下才发现问题有多大：

| | 大小 | 占比 |
|---|---|---|
| theme.css 总计 | 487.1 KB | 100% |
| 其中说明性注释 | 241.4 KB | **49.6%**（1012 块） |
| 剥离后 | **261.7 KB** | — |

注释对读源码的人有价值，但对最终产物没有意义 —— 浏览器每次加载都要完整解析一遍，
用户也不会去读它。所以构建默认剥离说明性注释。

**三类注释必须保命**（剥离时会漏功能或漏测试，已在代码里写明）：

1. **Style Settings 配置块** —— 它本身就是一条注释，插件靠它识别主题设置，漏了设置面板就空了；
2. **模块横幅**（36 条 `模块：xxx` ）—— 用于在产物里定位模块归属，测试也靠它断言
   「src 里每个模块都进了产物」；
3. 文件头 —— 由构建单独拼接，不经过剥离。

剥离器是**字符串感知的状态机**，不是正则：CSS 的 `content` 属性里可能出现任意字符，
正则一把梭会误伤。剥离后另做了两道复核 —— 产物里的 `@settings` 块用真实 js-yaml
重新解析（MoYun（墨韵）/ 36 项，通过），`模块：` 横幅计数为 36。

需要带注释的版本：`node build.mjs --comments`（对照源码阅读时用）。

验证：`npm test 28` / `verify 59×2` / `check-mermaid` 全过 —— 注释不影响任何计算样式，
但把「产物是否陈旧」检查也跑了一次，确认提交的产物与源码一致。
## [1.1.5] - 2026-09-16

### 重写 README：中英双语主题自述（目录的硬性要求）

目录校验连续两次提示 `README does not appear to contain English text` ——
第一次我只加了一节 354 词的英文小节，没用。量了一下才知道为什么：
那时 README 里**汉字 3241 个、拉丁字母 4195 个**，正文主体仍是中文，
任何「整篇语言判定」或「开头须为英文」的规则都不会通过。

按目录的原意重做（它的读者主要在英文社区，要求的是英文主题自述）：

| | 之前 | 现在 |
|---|---|---|
| 汉字 / 拉丁字母 | 3241 / 4195 | **737 / 5771** |
| 汉字占比 | 约 27% | **8%** |
| 英文内容 | 一节 354 词 | 完整英文自述（为什么、六项特性、安装、设置、开发、局限、许可） |
| 中文内容 | 全文（约 350 行） | 速览 + 指向全文 |
| 赞助 | 第一章 | 第一章（中英双语，位置不变） |

结构：标题块（中英双标题）→ **Sponsor / 赞助（双语）** → **English**（主体）→ **中文说明**（速览）。

原中文全文**一字未删**，完整搬到 **[docs/zh/README.md](docs/zh/README.md)**，
并在搬运时修掉所有因换目录而失效的相对链接（`docs/…`、`LICENSE`、截图等改为上跳两级），
文首加了一句指回英文 README 的说明。

### 说明：关于「主题 CSS 体积偏大」的告警

`theme.css` 约 487 KB / 36 个模块，属提示级。体积主要来自源码注释（构建产物保留注释，
便于用户与贡献者直接阅读构建结果）。这是刻意的取舍，暂不通过剥离注释来消警告。
## [1.1.4] - 2026-09-16

### 修复：提交社区目录被打回（CSS parse error）

Obsidian 目录校验结果：**Failed**，一个硬错误 ——

```
CSS parse error: undefined
theme.css:10888
```

根因：插入「极简 · 黑白线框」模块时，edit 的替换把 `九、打印` 注释块的开头标记吃掉了，
只剩一行悬挂的「星号 + 九、打印：…」和一个孤立的结束标记。CSS 解析器把它当成
选择器的一部分，于是解析失败。**花括号仍然成对**，所以构建期现有的括号配对检查
发现不了它 —— 检查的维度少了一个。

修法与防线：

1. 恢复注释块结构；
2. 构建期新增**注释结构检查**（状态机扫描）：
   - 不在注释里却遇到结束标记 → 「多余的注释结束符」；
   - 文件结束时仍在注释里 → 「注释未闭合」。
   并做了反向验证：故意注入同样损坏 → 构建失败；还原 → 通过。

### 修复：同一规则内重复声明（校验器告警的那处）

`Unexpected duplicate "line-height" theme.css:3169` 是真问题：公文表格里同时写了
`line-height: 1.6` 与 `1.5`，**后者生效、前者白写** —— 改样式时的残留。
已全仓扫描（36 个模块里只有这一处），修正为 1.6，并把「同一规则内同一属性声明多次」
纳入构建期提示级检查：它不报错、不影响解析，只会让人以为写的值生效了。

### 排查中发现：我自己也犯了一次同类错误（已加检查）

比对 Release 资产时发现本地/仓库里的 `theme.css` 头部仍写着 v1.1.3 —— **改了 manifest
的版本号却没重新构建**。功能无影响，但「仓库里的产物」与「清单」对不上。

由此新增 **产物陈旧检查**（只在 `--check` 模式下跑，也就是 CI 那条）：
把「源码 + 清单」与「已提交的 theme.css」逐字符比对，不一致就报错并提示重新构建。
加好之后先拿自己的疏漏验证了一遍：确实报出「首个差异在第 113 个字符附近」，重新构建后通过。

### 新增：README 英文说明（目录硬性要求）

校验器指出：`README does not appear to contain English text` —— 社区目录面向英文用户，
必须有英文说明。新增 `## English` 一节（约 350 词，置于赞助小节之后、中文正文之前），
讲清主题定位、与英文主题的差异、六项特性、安装方式与许可。

### 关于其余告警（均为提示级，不阻塞）

| 告警 | 说明 |
|---|---|
| `!important` 约 120 处 | 全部在 Mermaid 模块：对手是 SVG 内部的 ID 选择器样式，`!important` 是唯一手段（模块内有预算机制与理由说明） |
| `:has` 若干处 | 表格转 block 的兜底、图片段落与编辑区行的豁免，都是刻意使用 |
| 浏览器特性「仅部分支持 Obsidian 1.5.8」 | 校验器以 1.5.8 为基线，本主题 `minAppVersion` 为 1.6.0 |
| `mjx-container` 未知类型选择器 | MathJax 的自定义元素，合法 |
| 主题 CSS 体积偏大 | 487 KB / 36 个模块，属提示 |
## [1.1.3] - 2026-09-16

### 修复：首行缩进在编辑区完全失效（用户报告）

用户反馈「首行缩进 2 格貌似失效了」。追下去发现是两个独立问题：

**① 编辑区从来就没有缩进（真空档，不是回归）**

主题的首行缩进在两种视图里是两套规则：

| 视图 | 触发条件 | 公文模式下 |
|---|---|---|
| 阅读视图 | `body.my-gov-style .markdown-rendered p` | ✅ 有缩进 |
| 编辑区（实时预览） | `body.my-indent-on .cm-line` | ❌ **没有** |

「公文风格」默认**开**且说明里承诺「首行缩进两字」，而「首行缩进两字」开关默认**关** ——
于是编辑区一条缩进都没有。缩进是「读」的属性，两种视图不该各说各话。
现在触发条件统一为 `:is(.my-indent-on, .my-gov-style)`：开缩进开关或开公文模式，两处一致。

**② 豁免清单把「带内联格式的段落」整行跳过了**

编辑区豁免清单里原本有 `:has(.cm-formatting)`。它的原意是排除「显示原始标记的行」，
但 Live Preview 里**任何含内联标记（加粗 / 斜体 / 链接 / 行内代码）的段落**，DOM 里都留着
`.cm-formatting` 元素（只是被 Obsidian 用 CSS 藏起来），于是这些段落反而不缩进 ——
表现为「有的段落缩了、有的没缩」。现已去掉：带行内格式的段落仍然是正文段落。

### 为什么这个空档能活这么久：验证台根本没有编辑器 DOM

验证台此前**只搭了阅读视图**，所以「编辑区的缩进规则」从来没有被任何检查覆盖过 ——
写错了也不会有人发现。现已补上 CodeMirror 的真实 DOM 形状（`.cm-content > .cm-line`，
含普通行 / 含内联标记的行 / 标题行 / 列表行 / 引用行 / 代码行），用 `visibility:hidden`
保持布局（拿到真实 used 值）但不干扰截图。

顺带补了「松列表」（`li` 内直接写段落）这一形态 —— 主题专门为它写了豁免规则，
而夹具里原先只有紧列表，那条规则同样从未被覆盖。

验证断言 53 → **59 项**，新增 6 条全部围绕缩进（含「两个开关都关时才不缩进」的反向用例）。
## [1.1.2] - 2026-09-16

### 修复：`fundingUrl` 的锚点在 GitHub 上是失效的

1.1.1 把 `fundingUrl` 指向 `README#sponsor`（配 `<a id="sponsor">`）。抓 GitHub
渲染后的 HTML 核对发现：**锚点 id 被改写成了 `user-content-sponsor`**，
所以 `#sponsor` 这个 fragment 在 GitHub 上根本跳不到 —— 点「赞助」按钮会落在页面顶部
而不是赞助小节。这类失效不报错、不告警，只有真的点一次才会发现。

改为指向仓库内的独立赞助页 `SPONSOR.md`（无 fragment，链接天然稳定）。

### 新增：三个赞助入口，链接统一

| 入口 | 位置 | 指向 |
|---|---|---|
| Obsidian 社区主题卡片的「赞助」按钮 | `manifest.json` 的 `fundingUrl` | `SPONSOR.md` |
| GitHub 仓库页的「Sponsor」按钮 | `.github/FUNDING.yml` | `SPONSOR.md` |
| README 首个章节 | `README.md` 标题块之后 | 收款码直接可见，并链到 `SPONSOR.md` |

三处指向同一个地址，以后换收款方式只需动 `docs/sponsor/` 与这一处链接。

`SPONSOR.md` 除收款码外写清了「这笔钱会花在哪」与「不花钱也能帮上忙」——
后者不是客套：深色模式表格、Mermaid 隐形字这两个真 bug 都是用户报出来的。
## [1.1.1] - 2026-09-16

### 新增：赞助入口

用户在浏览器插件项目里已有微信/支付宝收款码，把它接进主题的三处入口：

1. **`manifest.json` 增加 `fundingUrl`** —— 指向 README 的赞助小节。
   Obsidian 社区主题卡片读到该字段会显示「赞助」按钮（参考 Minimal 主题同款写法）。
2. **README 新增「赞助」小节**，两张收款码并排，配稳定的 `#sponsor` 锚点。
3. **每次 Release 说明后附收款码** —— 下载页面是最常被看到的地方。
   实现走了一步弯路：先用了 release action 的 `append_body`，实测它与
   `generate_release_notes` 同时使用时会被【静默忽略】（1.1.1 的说明里只剩一行
   Full Changelog）；改成发布后用 `gh release edit` 读回 body → 拼接 → 写回，
   确定且可验证。1.1.1 的说明已手工补齐。

收款码放在 `docs/sponsor/`（`wechat-pay.jpg` / `alipay.jpg`）。

### 顺带发现：浏览器插件里的两张收款码是反的

复制图片时核对内容发现，`Readmdvue` 的 `DonationModal.vue` 把两张图接反了：

```ts
import wechatPayImgUrl from '../assets/pay/24d4be73ee….jpg'  // 实际是【支付宝】蓝色海报
import alipayImgUrl   from '../assets/pay/8832d51234….jpg'  // 实际是【微信】绿色海报
```

模板里写的是「微信支付」标签配 `wechatPayImg`，于是弹窗会把支付宝的码显示在
「微信支付」标题下面（反之亦然）。钱最终都会到账，但用户扫的码和看到的标签不一致。
本主题按【图片实际内容】命名，未受影响；插件那边的两行 import 建议对调。
## [1.1.0] - 2026-09-16

### 新增：图表风格扩成「一族六款」

用户提出：主题有八大韵色，Mermaid 却只给了一个赛博霓虹，太单薄。于是把图表风格
做成一个家族 —— 骨架（描边/发光/画布/网格/字号）只写一遍，配色抽成可切换的变量组，
**加一套配色 = 十来行令牌，而不是复制一整层 CSS**。

| 风格 | 画布 | 主色 | 次色 | 定位 |
|---|---|---|---|---|
| 墨韵（默认） | 透明，落在纸面/夜色 | 跟随「主题色」八韵 | — | 与正文同源，随明暗与韵色变化 |
| 赛博 · 青蓝 | 深色终端 + 网格 | 霓虹青 | 品红 | 默认霓虹 |
| 赛博 · 矩阵翠绿 | 深色终端 + 网格 | 荧光绿 | 酸绿黄 | 最「黑客」的一套 |
| 赛博 · 紫粉 | 深色终端 + 网格 | 紫罗兰 | 品红 | 合成波（synthwave） |
| 赛博 · 琥珀 | 深色终端 + 网格 | 琥珀橙 | 珊瑚红 | 暖色终端，夜间长文更柔和 |
| 极简 · 黑白线框 | 透明 | 正文墨色 | 次级墨色 | 无颜色无辉光，打印与正式公文 |

**墨韵风格本来就跟着八韵走**：描边用强调色、辉光取自 `--my-accent-rgb`，
所以在「🎨 配色 → 主题色」里选朱砂，图里的框和光就一起变红。这一点现在写进了选项标签
（「墨韵（跟随主题色 · 八韵通用）」），不再需要用户猜。

极简线框刻意做成「无光」：它要把基础层的辉光显式关掉（`filter: none` / `text-shadow: none`），
否则「极简」会漏光。

### 验证

- `check-mermaid` 覆盖 **6 风格 × 2 明暗 × 6 类图 = 12 组**，逐元素对比度全部 ≥ 4.5:1
  （实测最低：墨韵 7.36 / 青蓝 5.99 / 矩阵 10.23 / 紫粉 7.42 / 琥珀 7.74 / 极简 8.83）
- 像素对拍逐款确认计算样式与屏幕像素一致（青蓝 #0b131d、翠绿 #08130e、紫粉 #120c22、琥珀 #17100a）
- 顺带修掉一个检查自身的假失败：极简风格的节点填充是 `transparent`，「计算透明、屏幕是纸色」
  并非缺陷；像素对拍现在会跳过非不透明填充，不再误报
## [1.0.9] - 2026-09-16

### 修复：Style Settings 打不开，报 YAMLException（用户报告）

用户点开 Style Settings 面板时看到：

```
Error: MoYun（墨韵）
YAMLException: duplicated mapping key in "MoYun（墨韵）" (247:5)
No style settings found
```

**根因**：1.0.8 新增「图表风格」时，插入点选错了 —— 落在「✨ 内容增强」分组头
与它的后续键之间，于是分组头的 `description` / `type` / `level` / `collapsed`
被挂到了新设置项上，同一个映射里出现两个 `description`、两个 `type`。
YAML 不允许重复键，插件解析直接抛异常，**整个设置面板失效（36 个设置项全部消失）**。

**更该被批评的是**：当时的构建检查全绿放行。它对 `@settings` 块只查了
「块存在」「有 name」「有 id」——粒度完全跟不上手脚。

### 新增：Style Settings 配置块结构校验（零依赖）

新建 `tools/lib/settings-parse.mjs`，把这个配置块按 YAML 的映射规则扫一遍：

| 级别 | 抓什么 |
|---|---|
| 错误 | 同一映射内重复键、缺 id / 缺 type、type 不在已知取值内、id 重复、`class-select` 缺 `options`、数值型 default 缺失/非数字/越出 min–max |
| 提示 | 无法解析的行、未知字段名（拼写错误）、设置项缺 title |

两个刻意的设计：

1. **不引入 js-yaml**。主题的构建与静态校验保持零依赖，任何机器 `node build.mjs`
   就能跑；同时按缩进分层记录键，遇到新的列表项先清掉更深的层级，
   因此嵌套 `options` 里重复出现的 `- value` 不会被误判。
2. **构建期即失败**。这次是「配置错了但构建通过」，所以校验必须挂在构建上，
   而不是靠人记得单独跑一次检查。

回归测试从 24 项增至 28 项，把这次的翻车写法原样固化成用例；
另用真实 `js-yaml` 独立验证过一遍：36 项、类型分布正常、赛博选项结构完整。
## [1.0.8] - 2026-09-16

### 新增：Mermaid 双风格 —— 墨韵（纸面辉光） / 赛博霓虹

用户提出两点：文字与线要「亮晶晶」，并且希望有赛博风格的图表主题。
查证后确认 Mermaid 官方只有 default / neutral / dark / forest / base 五套主题，没有霓虹系，
所以赛博风格由主题自己实现。设置面板新增「图表风格」开关：

| 风格 | 画布 | 描边 | 次级信息 | 文字 |
|---|---|---|---|---|
| **墨韵**（默认） | 透明，落在纸面/夜色上 | 强调色（随「韵色」变化） | 中性线 + 柔光 | 正文色 + 同色辉光 |
| **赛博霓虹** | 深色终端 + 青蓝网格底纹 | 霓虹青 `#22e0ff` + 外发光 | 品红 `#ff3d9a` | 冷白 `#eafcff` + 镭射光 |

**「亮晶晶」的实现**：不是加粗（汉字加粗会糊笔画），而是给文字与线加一圈同色柔光。
两条路径各司其职 —— SVG 的 `<text>` 用 `filter: drop-shadow()`（SVG 没有 text-shadow），
`foreignObject` 里的 HTML 标签用 `text-shadow`（更便宜，且不引入新的渲染上下文）。
光晕颜色取自强调色，因此换「韵色」时整张图的光感跟着换；
浅色模式光晕「短而实」（3px / 32%），深色模式「长而亮」（7px / 60%）。

赛博风格的三个刻意决定：

1. **画布恒定深色，不跟随应用明暗。** 霓虹是「暗部里的光」，铺在浅色纸面上只会变成一堆刺眼彩线；
   所以这套配色放在令牌层的基础块而不是 `.theme-*` 里，浅色模式下它依然是那块深色画布。
2. **正文用冷白而不是彩色。** 彩色文字发光在中文小字号下会明显发虚；彩色只留给描边与次级信息。
3. **打印回落成素色线框。** 赛博是屏幕风格，纸面上既费墨又不清晰，`@media print` 里全部还原。

连线也一并提亮：浅色下保持中性（彩色线在纸面上太吵），深色下带一点强调色青光（58% 不透明度 + 辉光），
既「亮晶晶」又不抢走节点的视觉主角。

### 验证

- `tools/check-mermaid.mjs` 升级为 **2 种风格 × 明暗 2 模式 × 6 类图**，逐元素对比度全部 ≥ 4.5:1
  （实测最低：墨韵 7.36 / 赛博 5.99），并继续做反相滤镜断言与像素对拍
- `tools/verify.mjs` 断言 49 → 53 项，新增「SVG 文字有辉光」「HTML 标签有辉光」
  「赛博风格切换画布」「赛博节点描边为青蓝且带发光」四条 —— 都是真开类再实测，不是断言 CSS 里写了规则
## [1.0.7] - 2026-09-16

### 修复：Mermaid 图里的文字变成隐形字（用户报告）

用户反馈「mermaid 相关的图，里面的文字都显示不清了，应该是文字和背景颜色相同」。
查下去发现是三个独立缺陷叠在一起，任何一个单独存在都足以让文字消失：

1. **只覆盖了 `fill`，没覆盖 `color`。** Mermaid 注入的关键一条是
   `#mermaid-1 .label text, #mermaid-1 span, #mermaid-1 p { fill:#333; color:#333 }`。
   SVG 的 `<text>` 吃 `fill`，`foreignObject` 里的 HTML 标签吃 `color` ——
   只改一个，另一半仍是 Mermaid 的 `#333`。
2. **Obsidian 不切换 Mermaid 的明暗主题。** 从 `obsidian.asar` 提取到的真实初始化参数只有
   `themeVariables: { fontFamily: 'var(--font-mermaid)' }`，没有 `theme` ——
   哪怕深色模式，Mermaid 用的仍是自己的浅色变量。
3. **Obsidian 会给深色下的 Mermaid SVG 加反相滤镜：**
   `.theme-dark .mermaid > svg { filter: invert(100%) hue-rotate(180deg) saturate(1.25) }`。
   那是给「不做深色适配的图」的兜底；主题既然已经逐色接管，再反相一次就全部翻过来 ——
   深色节点底被反成刺眼白块，浅色文字被反成深色文字压在刚变白的块上。

修法与设计调整（按用户指出的方向：**Mermaid 的图主要是线与文字，背景是透明的**）：

| 项 | 原做法 | 现做法 |
|---|---|---|
| 画布 | 铺一层面板底色 | 透明，图形直接落在笔记纸面上 |
| 节点 | 填充抬升层（自造底色） | 只留强调色轮廓，填充用纸面色（不透明，可挡住穿过的连线） |
| 子图 | 米黄底 + 边框 | 透明 + 虚线轮廓，视觉层级低于节点 |
| 文字 | 只改 fill | `fill` 与 `color` 一起改，并覆盖 text/tspan/span/p/div 全部载体 |
| 边标签 | 只有文字色 | 文字色 + 背景色一起接管（Mermaid 默认刷 #e8e8e8/#ECECFF，深色下是亮斑） |
| 深色适配 | 依赖 Obsidian 反相 | 关闭反相，主题自己负责明暗 |

同时修复：甘特图日期轴用 `--my-text-faint` 只有 3.75:1，改为次级文字色（≥ 4.5:1）。

### 新增：Mermaid 真实渲染检查（tools/check-mermaid.mjs）

上一版的验证台夹具是**手写的静态 SVG**，并且用 `mermaid.initialize({ theme: 'dark' })`
渲染 —— 而 Obsidian 根本不传 `theme`。夹具与真实环境的这一点差异，让检查全绿却漏掉了
用户的 bug。**验证工具本身也会说谎，夹具必须复刻真实初始化参数。**

新检查用真实 Mermaid 包 + Obsidian 的真实初始化参数渲染六类图（流程图含 subgraph 与换行标签、
时序图、状态图、类图、饼图、甘特图），然后：

- 逐个文字元素计算「可见文字色 / 有效背景色」对比度（SVG 看 fill、HTML 看 color），低于 4.5:1 即失败；
- 断言 SVG 上没有被施加反相滤镜；
- **像素对拍**：读取元素截图里的真实像素，与计算样式比对 —— 计算说「深底浅字」而屏幕是
  「白底深字」时必须失败（`filter` 只改像素、不改 `getComputedStyle`，这正是此次漏检的原因）；
- 每张图单独截图留证（tools/preview/out/mermaid-<模式>-<图类型>.png）。

本机无 mermaid 包时该检查会明确跳过而不是假装通过（主题保持零依赖）。

### 工程

- 静态夹具同步升级为复刻 Mermaid 真实注入规则（含 `span/p` 的 color 与边标签背景色）
- 验证断言 47 → 49 项（新增 HTML 标签 color 路径、边标签背景、容器透明）
## [1.0.6] - 2026-09-16

### 新增：按分辨率自适应字号（用户报告）

用户反馈：高分屏上文字显得太小，「看起来眼睛会瞎」。

根因是字号被写成了固定 px。同一段 16px 正文，在 1080p（1920×1080 / 24 寸）上
字高约 4.4mm，在 4K（3840×2160 / 27 寸）上只剩约 2.5mm —— 而 CSS 拿不到物理 PPI，
屏幕越大字越小这件事，固定 px 无解。

做法是拿「窗口宽度」当分辨率的代理，并把基准值的所有权交还 Obsidian：

| 窗口宽度 | 正文字号 | 界面字号 | 一行字数 | 版心 |
|---|---|---|---|---|
| 1280 | 16.2px | 15.2px | 46 | 744px |
| 1920 | 17.6px | 16.5px | 46 | 809px |
| 2560 | 19.0px | 17.9px | 46 | 874px |
| 3840 | 21.8px | 17.9px | 46 | 1003px |

- 基准取 Obsidian 原生的 `--font-text-size`（即「设置 → 外观 → 字体大小」），
  主题不再另立一个私有 px 值
- 增益为 `clamp(0px, (100vw - 1200px) × 0.0022, 6px)`：1200px 以下不放大，
  3840px 时约 +5.8px；由 Style Settings 的「分辨率自适应」滑杆控制（0～200%，0 关闭）
- 版心同步改成「一行 N 个汉字」模型（`--my-content-chars`，默认 46）：
  字号放大时版心一起变宽，任何宽度下都是一行 46 字，宽屏上不会变成窄窄一条
- 界面字号跟随正文但夹在 15～19px，防止把正文字号调大后整个界面失控

### 修复：Obsidian 自带的字体与字号设置被主题吞掉（用户报告）

用户反馈「在 Obsidian 里改了设置没反应」。核查确认主题有两处越权：

1. `obsidian-map.css` 直接覆盖 `--font-text` / `--font-interface` / `--font-monospace` /
   `--font-text-size`。Obsidian 的字体链路是
   `var(--font-text-override), var(--font-text-theme), var(--font-default)` ——
   覆盖第一项等于把用户在「外观 → 字体」里的选择整条吞掉；覆盖 `--font-text-size`
   则让「外观 → 字体大小」失效。
2. `typography.css` 反过来把 `--font-text-override` 指向主题字体 ——
   注释写的是「尊重用户选择」，代码做的却是相反的事。

现在主题只写 `*-theme` 层：用户没选就用墨韵的中文优先字体栈，选了就无条件让位。
主题自己的样式改走 `--my-font-*-effective`（用户 override 在前、主题栈兜底），
标题字体一并跟随。公文体下用户字体只接管正文，标题仍按公文层级 ——
「靠字体区分层级」本身就是公文特征。

### 新增：Mermaid 图表随主题（吸收自本机库里的两个 CSS 片段）

用户原有的 `wiki-mermaid.css` / `project-wiki-beautify.css` 已吸收进
`src/60-plugins/mermaid.css`，两个片段文件随即删除，`enabledCssSnippets` 清空。
保留的是它的正确做法（配色全部走主题变量，明暗自动成立），换掉的是它的写法问题：

| 原片段 | 现在 |
|---|---|
| 节点/连线/字号写死 | 全部走主题语义令牌，随明暗与字号变化 |
| `font-size: 14px` 死值 | `calc(var(--my-font-size-base) × 0.875)`，跟着分辨率放大 |
| 无深色适配 | 只写一份，两种模式自动成立 |
| 图形超出被裁切 | 容器横向滚动 + `max-width: 100%` |
| 覆盖 frontmatter、H1/H2 上色、斑马纹等 | 不吸收 —— 与公文体和主题语言冲突 |

Mermaid 会把配色内联成 SVG **内部**的 `<style>#mermaid-1 .node rect{...}</style>`，
那是 ID 选择器，外部 CSS 只能靠 `!important` 压过。为此构建新增
`@important-budget` 预算机制：模块可在文件头声明预算并写明理由，超预算才告警；
写了标记但没写理由直接报错 —— 不允许无理由开口子。

### 工程

- 验证台新增 Mermaid 夹具（忠实复刻 Mermaid 的内联 ID 样式注入），实测覆盖是否真的生效
- 验证断言从 35 项增至 47 项，新增三条分辨率自适应实测（字号变大 / 版心同步变宽 / 一行始终 46 字）
- 模块数 35 → 36
## [1.0.5] - 2026-09-16

### 新增：公文风格（默认开启）

依据 GB/T 9704-2012《党政机关公文格式》新建 `src/15-govdoc/govdoc.css`，
并设为**主题默认形态** —— 打开即是一眼可辨的公文排版：

| 要素 | 规范 | 实现 |
|---|---|---|
| 正文 | 三号仿宋_GB2312 | 仿宋字体栈 + Times New Roman 数字 |
| 一级标题「一、」 | 三号黑体 | H2 用黑体栈 |
| 二级标题「（一）」 | 三号楷体 | H3 用楷体栈 |
| 三级标题「1.」 | 三号仿宋加粗 | H4 |
| 文件标题 | 二号方正小标宋 | H1 居中 + 宋体栈 |
| 行距 | 每页 22 行 | 沿用 1.8（屏幕适配） |
| 首行缩进 | 2 字符 | `text-indent: 2em` |
| 对齐 | 均等割付 | `text-align: justify` + `inter-character` |
| 表格 | 全框线 | 单元格四边描边、直角、无斑马纹 |
| 强调 | 加粗 / 着重号 | 去掉荧光笔底色，改用句下着重号 |

两处刻意偏离规范并已在注释中说明：正文字号不强制 16pt（屏幕 DPI 与纸张不同），
版心宽度不固定 156mm（屏幕宽度差异大）。**打印时全部回归规范**：
`@page` 设为 A4 + 37/26/35/28mm 页边距，正文 16pt / 行距 1.78 / 标题 22pt。

深色模式：**排版规范一条不改**，仅替换纸墨颜色 —— 夜间依然是「公文」而不是另一种风格。

设置面板新增「📄 排版基调」分组并置于最顶部，公文开关默认开启，一键可切回通用排版。

### 修复：深色模式的结构对比度（用户报告）

用户反馈深色下表格式糊、层次不清。实测数据：

```
表格行分隔线   rgba(188,182,169, 0.08)   ← 8% 不透明度，深色底上几乎不可见
表头底色       rgb(27,25,22) vs 行 rgb(23,21,18)   ← 只差 4 个色阶
代码注释       #6e685e                   ← 对比度约 3.5:1，低于 AA 的 4.5:1
```

根因是深浅双模式的经典错误：**两套用了相同的 alpha**。浅色下 8% 的黑是清晰的浅灰，
深色下 8% 的白却几乎不可见。修复：

- 深色模式的边框 alpha 整体提高（`--my-border` 0.14→0.22、`--my-border-strong` 0.24→0.36、
  `--my-border-subtle` 0.08→0.13、`--my-divider` 0.11→0.18）
- 表格行分隔线由 `--my-border-subtle` 改用 `--my-border`
- 新增 `--my-table-head-bg` 令牌，深色用叠加色（实色阶在深色下只差 4 个色阶，肉眼无感）
- 代码注释提亮至 `#8a8478`（达 AA 级）

### 修复：公文体下标注丢失语义色（自查发现）

给公文体写标注样式时把 `--callout-color` 误当作 RGB 三元组，写成
`rgb(var(--callout-color))`。实际上它是**普通颜色值**（app.css 里形如 `#086ddd`），
对 hex 套 `rgb()` 完全无效 —— 结果六种标注在深色下全变成一模一样的黑框，
warning 与 danger 无法区分。

> 这是同一类错误的第二次（首次是 `hsl(var(--my-*-rgb) / α)`）。教训：
> **「严肃化」应当去掉装饰（圆角、底纹），而不是去掉语义** ——
> 颜色是标注的核心信息载体。

### 新增构建期校验（防止第三次）

新增规则：以 `-rgb` / `-hsl` 结尾的变量是三元组（允许被 `rgb()` / `hsl()` 包裹），
**其余颜色变量一律不得包裹**，否则报错。已实测该规则能拦住本次的错误写法。

### 测试

验证台从 28 项增至 **35 项**：

- 5 项断言改为按风格分流（圆角、色条、字重、代码块底色在公文/通用下期望值相反）
- 新增 5 项公文规范断言：仿宋字体、首行缩进、两端对齐、全框线表格、标题靠字体分层

## [1.0.4] - 2026-09-16

### 修复：表格远窄于版心（用户报告）

现象：阅读视图里的表格远窄于版心，右侧留出大片空白，看上去「又长又空」。

实测定位（验证台测量 4 列表格）：

```
版心内容区宽度   1042px
修复前每列宽度     83.6px   →  4 列共 334px，只占版心 28%
修复后每列宽度    205/205/410/205px  →  共 1026px，占版心 98.6%
```

三个叠加的原因，其中两个是主题自身引入的：

1. **`display: block` 的隐藏代价（主因）** —— 此前为了让宽表格能横向滚动，给
   `table` 设了 `display: block; overflow-x: auto`。但 `<table>` 一旦变成块级容器，
   浏览器会为它生成一个**匿名表格盒子**来排列单元格，而那个盒子的宽度仍由内容
   决定。结果是边框、底色撑满了版心，单元格却挤在左边一小条。
   改为 `display: table; width: 100%`，让浏览器把版心宽度按各列内容比例分配。

2. **误设的列宽上限** —— `--table-column-max-width` 被设为 `46ch`（约 23 个汉字），
   而 Obsidian 原生值是 `none`。已恢复原生语义。

3. **过严的单格宽度上限** —— `td/th` 上有 `max-width: 24em`，进一步把列卡窄。已移除。

### 宽表格的退路

撑满不适用于列数很多的表格（会把每列压到两三个字、读不成句）。因此新增
`:has()` 分流：列数 ≥6 时退回「按内容宽度 + 可横向滚动」，其余情况一律撑满。

### 回归防线

验证台新增 2 条断言（现共 30 项），其中第二条专门抓「边框撑满、内容挤在左边」
这个具体缺陷，避免以后换写法时重犯：

- 表格撑满版心（≥ 内容区的 95%）
- 表格列宽总和撑满（防止「边框撑满、内容挤在左边」）

> 注：断言的基准必须是容器的**内容区**宽度。版心有 64px 左右内边距，
> 用外框宽度当基准会把「已经撑满」误判为没撑满 —— 这个坑在本次修复中也踩到了。

## [1.0.3] - 2026-09-16

### 主题改名（为符合官方目录的命名规范）

提交到 Obsidian 社区目录时被拒：`This name is not allowed in the directory.`
查官方 manifest 规范后确认原因 —— 规范原文要求：

> - Prefer English names and use **Basic Latin** characters only. No punctuation
>   (except hyphens, plus sign, and parenthesis), emoji, or special characters are allowed.
> - **Theme names cannot be changed once the theme has been submitted** to the community directory.

原名称「墨韵 MoYun」含中文字符，不合规。现改为 **MoYun**（拼音，保留品牌发音；
「墨韵」作为中文名保留在 README 与文档中）。

> 这一条不可逆：官方规定主题一旦提交到目录就不能再改名，所以名称必须一次选对。

### 工程改进：主题名收敛为单一事实源

改名过程中发现主题名被硬编码在 4 个地方（构建产物头部、演示库目录名、
`cssTheme` 配置、脚本与测试里的路径），漏掉任何一处都会导致主题失联。现已全部改为
从 `manifest.json` 读取：

- `build.mjs` 的产物头部与日志输出读 `manifest.name`
- 演示库的主题目录名与 `appearance.json` 的 `cssTheme` 由构建同步（新增，此前会漏）
- `tools/make-demo-vault.mjs` 与 `tests/` 不再硬编码路径
- `make-demo-vault.mjs` 的路径改为基于脚本位置推导，不再依赖当前工作目录

### 数据

- 官方目录 758 个主题中名称含中文的为 **0 个**，本主题原先会是唯一一个（现已改为拼音）。

## [1.0.2] - 2026-09-16

### 为提交官方社区目录做准备

- **主题商店封面图**：新增 `screenshot.png`（夜间）与 `screenshot-light.png`（日间），
  均为官方向导推荐的 512×288 的 2 倍图。官方要求提交时仓库根目录必须有展示截图。
  配套新增 `tools/preview/cover.html` 与 `tools/make-screenshot.mjs`，主题改版后可一键重出。
- **manifest 精简**：移除值为空字符串的 `fundingUrl`。
  官方目录里已收录的 758 个主题无一把该字段留空 —— 省略优于空值。
- README 顶部接入封面图，徽章版本号同步。

### 兼容性说明（本次核查结论）

- 主题未使用 CSS 嵌套语法，全部现代特性均为优雅降级：
  `:has()`、`color-mix()` 在 Obsidian 1.6（Chromium 120）即可用；
  仅 `text-autospace`（中西文自动间距）需要 Obsidian 1.13+，低版本会静默忽略该属性，
  其余排版特性不受影响。故 `minAppVersion` 保持 `1.6.0`。

## [1.0.1] - 2026-09-16

### 元数据修正

- **作者归属**：`manifest.json` 的 `author` 改为实际维护者 `YaYII`，并补上 `authorUrl`。
  v1.0.0 里是占位文本「墨韵主题项目」。
- 主题的功能与样式与 v1.0.0 **完全一致**（`theme.css` 内容仅有头部版本号变化）。

### 构建流程（仓库维护相关，不影响使用者）

- 移除产物头部的生成时间戳，使构建**可复现**：相同源码现在产出逐字节相同的文件。
  此前 CI 构建的产物与本地永远无法对上，也无法用哈希校验发布资产。
- 模块排序从 `localeCompare`（依赖运行环境 ICU 版本，跨 Node 版本可能乱序）
  改为码位序直接比较，消除 CI 与本地分叉的隐患。
- 补充发布后校验流程文档：用 GitHub API 的 `digest` 字段校验资产一致性，
  而不是用 `/releases/latest/download/` 下载链接（该路径有 CDN 缓存，会拿到旧文件）。

## [1.0.0] - 2026-09-16

首个正式版本。

### 中文排版（本主题的核心）

- **中西文自动间距**：基于 CSS Text Level 4 的 `text-autospace`，在中文与英文、数字之间自动插入约 1/8 全角间隙。零 JS、不污染原文。
- **首行缩进**：`text-indent: 2em`（不是社区常见的 `2ch` —— 实测 `2ch` 在 16px 下只有 1.11 个汉字宽，缩进明显不足）。精确豁免列表、引用、标注、表格、代码、数学块、图片段落、脚注、嵌入笔记与所有标题，并单独处理实时预览的 CodeMirror 行语义类。
- **行高与版心**：默认行高 1.8、版心 46rem，可在设置中调整。
- **标题字重 650**：避免汉字笔画在 700 以上字重时粘连；标题使用 `text-wrap: balance` 平衡折行。
- **不用伪斜体**：汉字无真斜体，改用「变色 + 半粗」，并提供中文传统的**着重号**（`text-emphasis: dot`）作为可选方案。
- **段落寡行控制**：`text-wrap: pretty`（实测对中文有效，可把尾行孤字上移一行）。
- **六种中文排版模式**：`my-govdoc` 公文（参照 GB/T 9704-2012）、`my-poetry` 诗词、`my-thesis` 论文、`my-letter` 信纸、`my-bigscreen` 大字投屏、`my-immersive` 沉浸写作。

### 配色

- **八大韵色**：青花（默认）、朱砂、竹青、藤紫、秋香、黛青、胭脂、松烟。全部取自中国传统色并按对比度校准。
- **日间「宣纸」**：底色不用纯白，用带暖调的宣纸色。
- **夜间「墨夜」**：正文不用纯白，用宣纸色文字压在深墨底上，降低夜间阅读的对比冲击。
- **三层设计令牌**：原始色板 → 语义令牌 → Obsidian 原生变量映射（覆盖 778 个原生变量）。

### 组件与界面

- 标注（Callout）：全部内置类型配色，含折叠态、嵌套层级，以及可选的扁平样式。
- 表格：表头底色、极轻斑马纹、更大的中文单元格内边距、悬停行高亮。
- 代码块：语法高亮配色随明暗模式成对切换，复制按钮与语言标签。
- 列表：项目符号降尺寸、等宽有序编号、缩进引导线、任务复选框完整状态。
- 布局：三层表面层次（应用底 / 侧栏 / 纸面），标签页与内容区「无缝连通」以消除视觉断层，拖拽分隔条高亮反馈。
- 设置界面：说明文字提高对比度（中文用户最需要读这里的字），Style Settings 多层折叠用缩进与色条建立纵深。
- 增强特性：彩虹文件夹、卡片化列表、图片自动网格、专注模式、标题自动编号、六色高亮、`is-flashing` 柔和渐隐。
- 插件适配：Dataview、Tasks、Kanban、Excalidraw、Canvas、Calendar、Charts、Admonition、Style Settings 等。

### 设置面板

- 30 多个选项，**全部中文说明**，并且每组都写清取舍而不只是给个滑杆。
- 六组分类：配色 / 中文排版 / 字体 / 行距与版心 / 界面细节 / 内容增强。
- 附带五套预设（宣纸、墨夜、竹简、秋香、素笺）。

### 工程化

- **模块化源码**：34 个模块，构建产出单一 `theme.css`。